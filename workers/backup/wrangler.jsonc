{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "nexus-backup-disabled",
  "main": "src/index.ts",
  "compatibility_date": "2026-08-11",

  // Deliberately no R2 bucket, service bindings, routes, vars, or secrets.
  // Phase 8 is closed until a separate KDF/backup security ADR is reviewed
  // and explicitly accepted. This scaffold must not be deployed as a backup API.
  "workers_dev": false,
  "preview_urls": false,
}
