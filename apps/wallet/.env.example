# The public, cookie-free Nexus edge API. This loopback value is for `vite dev` only.
# Production builds default to https://nexus.rowo.link and reject non-HTTPS values.
# The build emits dist/_headers with this exact origin as its sole remote connect-src.
VITE_NEXUS_API_URL=http://localhost:8787
